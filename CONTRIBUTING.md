# Contributing

欢迎通过 Issue 或 Pull Request 改进项目。提交前请：

1. 不提交 API Key、Cookie、视频、逐字稿、个人笔记、运行状态或私人路径；
2. 保持单视频安全边界，不加入静默批量下载、DRM 绕过或共享 Cookie；
3. 为行为变化增加测试；
4. 运行 `python -m pytest`、`python -m pip check` 和 `python -m compileall -q src`；
5. 不用模拟结果声称完成真实平台或付费 Kimi 验收。

安全漏洞请按 [SECURITY.md](SECURITY.md) 私密报告，不要提交公开 Issue。
